name: Deploy Image Bot

on:
  workflow_dispatch:  # Allows manual triggering
  schedule:
    - cron: '0 */6 * * *'  # Runs every 6 hours to keep alive

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 30  # Increase timeout for model download
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y ffmpeg

    - name: Install Python dependencies
      run: |
        pip install --upgrade pip
        pip install -r requirements.txt

    - name: Create .env file
      run: |
        echo "BOT_TOKEN=${{ secrets.BOT_TOKEN }}" > .env
        echo "USER_TAG=@AHMED_KHANA" >> .env
        echo "DEV_NOTE=بصفتي المطور" >> .env

    - name: Run Image Bot
      env:
        BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
      run: |
        echo "🔄 Downloading Stable Diffusion model (may take 5-10 minutes)..."
        python -c "
        from diffusers import StableDiffusionPipeline
        import torch
        print('✅ Stable Diffusion setup complete')
        "
        
        echo "🚀 Starting bot..."
        python bot.py
